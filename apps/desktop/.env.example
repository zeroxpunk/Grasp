# No persistent env vars are required right now.
# The desktop dev script injects VITE_DEV_SERVER_URL automatically.
# Uncomment this only if you want to point Electron at a custom renderer server.
# VITE_DEV_SERVER_URL=http://localhost:5173
