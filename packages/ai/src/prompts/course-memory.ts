export function buildInitialCourseMemory(title: string): string {
  return `# Course Memory: ${title}\n\nThis file tracks insights, learning preferences, and patterns as you progress through the course.\n`;
}
